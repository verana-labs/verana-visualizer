apiVersion: v1
kind: Service
metadata:
  name: {{ include "verana-visualizer.fullname" . }}
  labels:
    {{- include "verana-visualizer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "verana-visualizer.selectorLabels" . | nindent 4 }}
